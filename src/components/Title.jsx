import React from "react";

const Title = ({ title }) => {
	return (
		<div
			className="my-5
        "
		>
			<u>
				<h1 className="text-center ">{title}</h1>
			</u>
		</div>
	);
};

export default Title;
